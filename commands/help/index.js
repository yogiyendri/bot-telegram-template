const { help } = require('./help');
const { group } = require('./group');
const { channel } = require('./channel');
const { about } = require('./about');
const { system } = require('./system');

module.exports = { help, group, channel, about, system };